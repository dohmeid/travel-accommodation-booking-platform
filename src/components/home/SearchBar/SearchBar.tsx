import React, { useState, FC } from "react";
import classes from "./SearchBar.module.css";

const SearchBar: FC = () => {
  return (
    <>
      <p>SearchBar</p>
    </>
  );
};

export default SearchBar;
