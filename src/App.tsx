import React, { FC } from "react";
import "./App.css";
import LoginPage from "./components/LoginPage/LoginPage";

const App: FC = () => {
  return (
    <div className="App">
      <LoginPage />
    </div>
  );
};

export default App;
